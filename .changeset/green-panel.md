---
'@dalcvil/steam-green-react': minor
---

Add `GreenPanel`, a VGUI-style container with an optional uppercase title bar plus `inset` and `rounded` variants, ported from the upstream `.window` / `.box` rules.
